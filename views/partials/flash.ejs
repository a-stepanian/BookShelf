<% if(success && success.length) {%>
<div class="flash">
    <p><%= success %></p>
    <br>
    <div class="close">OK</div>
</div>
<% } %>

<% if(error && error.length) {%>
    <div class="flash">
        <p><%= error %></p>
        <br>
        <div class="close">Continue</div>
    </div>
<% } %>